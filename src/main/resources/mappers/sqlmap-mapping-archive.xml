<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tian.zone.dao.archive.ArchiveDAO">
	<select id="getYear" resultType="java.lang.String">
		select DISTINCT date_created from blog_blog a
	</select>
	<select id="getMonth" parameterType="java.lang.String" resultType="java.lang.String">
		select DISTINCT date_created from blog_blog a where a.date_created like '${year}%'
	</select>
</mapper>